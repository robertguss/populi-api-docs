# Note

> Populi API2 model. Source: https://populi.co/api/models/notes.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/organizations/(organization)/notes` | index (organization) | Retrieves all Note objects tied to a specific Organization. |
| GET | `/people/(person)/notes` | index (person) | Retrieves all Note objects tied to a specific Person. |
| POST | `/organizations/(organization)/notes` | create (organization) | Creates a new Note object. |
| POST | `/people/(person)/notes` | create (person) | Creates a new Note object. |

## The Note object

| Field | Required | Type |
|---|---|---|
| `id` | yes | int |
| `owner_type` | yes | enum (person, course, assignment, student, contact_org, contact, transaction, ledger_entry, todo, roster, bookstore_order, aid_year_award, room, transcript) |
| `owner_id` | yes | int |
| `content` | yes | text |
| `hidden` | no | bool |
| `added_at` | yes | datetime |
| `added_by_id` | no | int |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "note",
    "id": 14,
    "owner_type": "person",
    "owner_id": 1,
    "content": "They said on the phone they are deferring their enrollment.",
    "hidden": false,
    "added_at": "2018-01-30T00:04:04+00:00",
    "added_by_id": 2,
    "sandbox": true,
    "api_version": null
}
```

## index (organization): `GET /organizations/(organization)/notes`

Retrieves all Note objects tied to a specific Organization.

**Expandable:** `flag`, `file`

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/organizations/(organization)/notes" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 1,
    "results": 1,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "note",
            "id": 16,
            "owner_type": "contact_org",
            "owner_id": 1,
            "content": "Has 5 internship slots open for this summer.",
            "hidden": false,
            "added_at": "2020-01-30T00:04:04+00:00",
            "added_by_id": 2
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## index (person): `GET /people/(person)/notes`

Retrieves all Note objects tied to a specific Person.

**Expandable:** `flag`, `file`

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/notes" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 1,
    "results": 1,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "note",
            "id": 15,
            "owner_type": "person",
            "owner_id": 12,
            "content": "Called and set up a school tour.",
            "hidden": false,
            "added_at": "2020-01-30T00:04:04+00:00",
            "added_by_id": 2
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## create (organization): `POST /organizations/(organization)/notes`

Creates a new Note object.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `note` | yes | text |  |
| `is_private` | no | bool |  |
| `visibility_role_ids` | yes | array of Role ids |  |
| `is_flagged` | no | bool |  |

**Parameters:** You can optionally include a file upload with this route. Use a form post parameter called "file". Read about [request and response format](#request-and-response-format) to see how parameters need to be specified differently when there is a file upload.

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/organizations/(organization)/notes" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X POST
-d '{
    "note": "blah blah",
    "is_private": "1",
    "visibility_role_ids": "[7, 13, 14]"
}' \ 
```

Example response:

```json
"Example response not available."
```

## create (person): `POST /people/(person)/notes`

Creates a new Note object.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `note` | yes | text |  |
| `is_private` | no | bool |  |
| `visibility_role_ids` | no | array of Role ids | Default is [4] |
| `is_flagged` | no | bool |  |

**Parameters:** You can optionally include a file upload with this route. Use a form post parameter called "file". Read about [request and response format](#request-and-response-format) to see how parameters need to be specified differently when there is a file upload.

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/notes" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X POST
-d '{
    "note": "blah blah",
    "is_private": "1",
    "visibility_role_ids": "[7, 13, 14]"
}' \ 
```

Example response:

```json
{
    "object": "note",
    "id": 17,
    "owner_type": "person",
    "owner_id": 12,
    "content": "blah blah",
    "hidden": false,
    "added_at": "2026-09-21T18:04:53+00:00",
    "added_by_id": 22,
    "sandbox": true,
    "api_version": "2026-07-13"
}
```
