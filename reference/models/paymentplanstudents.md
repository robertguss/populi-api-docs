# PaymentPlanStudent

> Populi API2 model. Source: https://populi.co/api/models/paymentplanstudents.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/people/(person)/paymentplans` | index | Retrieves all PaymentPlanStudent objects tied to a specific Person. |
| POST | `/people/(person)/paymentplans` | create | Creates a new PaymentPlanStudent object. |
| GET | `/people/(person)/paymentplans/(paymentplanstudent)` | show | Retrieves a specific PaymentPlanStudent object. |
| DELETE | `/people/(person)/paymentplans/(paymentplanstudent)` | delete | Deletes an existing PaymentPlanStudent object. |

## The PaymentPlanStudent object

| Field | Required | Type |
|---|---|---|
| `id` | yes | int |
| `student_id` | yes | int |
| `academic_term_id` | yes | int |
| `invoice_id` | no | int |
| `payment_plan_id` | yes | int |
| `start_date` | no | date |
| `as_of_time` | no | datetime |
| `charge_types_broken_out` | yes | bool |
| `total_eligible_charges` | yes | decimal |
| `recurring_amount` | yes | decimal |
| `exclude_aid` | yes | bool |
| `scheduled_aid_handling` | yes | enum (ignore, consider_allocation_time, always_exclude) |
| `schedule_type` | yes | enum (days_from_applied, date, days_into_term, recurring) |
| `added_at` | no | datetime |
| `added_by_id` | no | int |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "payment_plan_student",
    "id": 7,
    "student_id": 12,
    "academic_term_id": 6,
    "invoice_id": 9,
    "payment_plan_id": null,
    "start_date": "2020-09-07",
    "as_of_time": null,
    "charge_types_broken_out": false,
    "total_eligible_charges": 0,
    "recurring_amount": 0,
    "exclude_aid": false,
    "scheduled_aid_handling": "consider_allocation_time",
    "schedule_type": "days_from_applied",
    "added_at": null,
    "added_by_id": null,
    "sandbox": true,
    "api_version": null
}
```

## index: `GET /people/(person)/paymentplans`

Retrieves all PaymentPlanStudent objects tied to a specific Person.

**Permissions:** One of the following roles is required to call this method: Financial Auditor, Financial Admin, Student Billing

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/paymentplans" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 1,
    "results": 1,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "payment_plan_student",
            "id": 7,
            "student_id": 12,
            "academic_term_id": 6,
            "invoice_id": 9,
            "payment_plan_id": null,
            "start_date": "2020-09-07",
            "as_of_time": null,
            "charge_types_broken_out": false,
            "total_eligible_charges": 0,
            "recurring_amount": 0,
            "exclude_aid": false,
            "scheduled_aid_handling": "consider_allocation_time",
            "schedule_type": "days_from_applied",
            "added_at": null,
            "added_by_id": null
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## create: `POST /people/(person)/paymentplans`

Creates a new PaymentPlanStudent object.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `payment_plan_id` | yes | int |  |
| `academic_term_id` | no | int |  |
| `recurring_amount` | no | decimal | This option is only relevant for plans with a recurring schedule type. |
| `start_date` | no | date |  |
| `as_of_time` | no | datetime |  |
| `invoice_ids` | no | array of Invoice ids | By default, all possible invoices will be added to the plan. Provide an array of invoice ids to limit the selection. |

**Permissions:** One of the following roles is required to call this method: Financial Admin, Student Billing

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/paymentplans" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X POST
-d '{
    "payment_plan_id": 1
}' \ 
```

Example response:

```json
{
    "object": "payment_plan_student",
    "id": 8,
    "student_id": 12,
    "academic_term_id": null,
    "invoice_id": null,
    "payment_plan_id": 1,
    "start_date": "2026-09-21",
    "as_of_time": "2026-09-21T18:05:04+00:00",
    "charge_types_broken_out": false,
    "total_eligible_charges": 0,
    "recurring_amount": 0,
    "exclude_aid": true,
    "scheduled_aid_handling": "consider_allocation_time",
    "schedule_type": "date",
    "added_at": "2026-09-21T18:05:04+00:00",
    "added_by_id": 22,
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## show: `GET /people/(person)/paymentplans/(paymentplanstudent)`

Retrieves a specific PaymentPlanStudent object.

**Parameters:** No additional parameters are needed beyond the ID of the object being requested that appears in the URL.

**Expandable:** `invoices`, `ignored_scheduled_aid`, `payment_plan`

**Permissions:** One of the following roles is required to call this method: Financial Auditor, Financial Admin, Student Billing

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/paymentplans/(paymentplanstudent)" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "payment_plan_student",
    "id": 7,
    "student_id": 12,
    "academic_term_id": 6,
    "invoice_id": 9,
    "payment_plan_id": null,
    "start_date": "2020-09-07",
    "as_of_time": null,
    "charge_types_broken_out": false,
    "total_eligible_charges": 0,
    "recurring_amount": 0,
    "exclude_aid": false,
    "scheduled_aid_handling": "consider_allocation_time",
    "schedule_type": "days_from_applied",
    "added_at": null,
    "added_by_id": null,
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## delete: `DELETE /people/(person)/paymentplans/(paymentplanstudent)`

Deletes an existing PaymentPlanStudent object.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `remove_initial_fee` | no | bool |  |

**Parameters:** No additional parameters are needed beyond the ID of the object to delete that appears in the URL.

**Permissions:** One of the following roles is required to call this method: Financial Admin, Student Billing

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/people/(person)/paymentplans/(paymentplanstudent)" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X DELETE
```

Example response:

```json
{
    "object": "payment_plan_student",
    "id": 8,
    "deleted": true
}
```
