# QuestionCategory

> Populi API2 model. Source: https://populi.co/api/models/questioncategories.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/catalogcourses/(catalogcourse)/questioncategories` | index | Retrieves all QuestionCategory objects tied to a specific Catalogcourse. |
| GET | `/catalogcourses/(catalogcourse)/questioncategories/(questioncategory)` | show | Retrieves a specific QuestionCategory object. |

## The QuestionCategory object

| Field | Required | Type |
|---|---|---|
| `id` | no | int |
| `catalog_course_id` | yes | int |
| `name` | yes | text (250) |
| `status` | yes | enum (active, retired) |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "question_category",
    "id": 1,
    "catalog_course_id": 591,
    "name": "Rude",
    "status": "active",
    "sandbox": true,
    "api_version": null
}
```

## index: `GET /catalogcourses/(catalogcourse)/questioncategories`

Retrieves all QuestionCategory objects tied to a specific Catalogcourse.

**Permissions:** One of the following roles is required to call this method: Academic Auditor, Academic Admin, Registrar

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/catalogcourses/(catalogcourse)/questioncategories" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 17,
    "results": 17,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "question_category",
            "id": 93,
            "catalog_course_id": 576,
            "name": "Rap Music",
            "status": "active"
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## show: `GET /catalogcourses/(catalogcourse)/questioncategories/(questioncategory)`

Retrieves a specific QuestionCategory object.

**Parameters:** No additional parameters are needed beyond the ID of the object being requested that appears in the URL.

**Permissions:** One of the following roles is required to call this method: Academic Auditor, Academic Admin, Registrar

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/catalogcourses/(catalogcourse)/questioncategories/(questioncategory)" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "question_category",
    "id": 100,
    "catalog_course_id": 576,
    "name": "Stuff",
    "status": "active",
    "sandbox": true,
    "api_version": "2026-07-13"
}
```
