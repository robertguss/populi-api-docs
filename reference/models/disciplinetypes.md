# DisciplineType

> Populi API2 model. Source: https://populi.co/api/models/disciplinetypes.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/disciplinetypes` | index | Retrieves all DisciplineType objects. |
| GET | `/disciplinetypes/(disciplinetype)` | show | Retrieves a specific DisciplineType object. |

## The DisciplineType object

| Field | Required | Type |
|---|---|---|
| `id` | yes | int |
| `name` | yes | text (100) |
| `report_as` | no | enum (warning, probation, suspension, expulsion) |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "discipline_type",
    "id": 5,
    "name": "Warning",
    "report_as": "warning",
    "sandbox": true,
    "api_version": null
}
```

## index: `GET /disciplinetypes`

Retrieves all DisciplineType objects.

**Permissions:** One of the following roles is required to call this method: Academic Admin, Discipline, Development

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/disciplinetypes" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 1,
    "results": 1,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "discipline_type",
            "id": 5,
            "name": "Warning",
            "report_as": "warning"
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## show: `GET /disciplinetypes/(disciplinetype)`

Retrieves a specific DisciplineType object.

**Parameters:** No additional parameters are needed beyond the ID of the object being requested that appears in the URL.

**Permissions:** One of the following roles is required to call this method: Academic Admin, Discipline, Development

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/disciplinetypes/(disciplinetype)" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "discipline_type",
    "id": 5,
    "name": "Warning",
    "report_as": "warning",
    "sandbox": true,
    "api_version": "2026-07-13"
}
```
