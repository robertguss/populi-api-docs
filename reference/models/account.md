# ChangeLog

> Populi API2 model. Source: https://populi.co/api/models/account.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/changelog` | changelog | Results are limited to changes in the past 30 days. |
| GET | `/lmssync/run` | Run LMS sync | Begin a sync operation between Populi and an outside LMS (e.g. |
| GET | `/logins` | logins | Results are limited to logins in the past 12 months. |

## The ChangeLog object

| Field | Required | Type |
|---|---|---|
| `id` | yes | int |
| `owner_type_id` | yes | int |
| `owner_id` | yes | int |
| `changed_by_id` | no | int |
| `changed_by_ip` | yes | text (50) |
| `changed_time` | yes | datetime |
| `type_id` | yes | int |
| `action` | no | enum (added, changed, deleted, printed, restored, cloned, requested) |
| `changed_from` | no | text |
| `changed_to` | no | text |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "change_log",
    "id": 946156314,
    "owner_type_id": 8,
    "owner_id": 1862,
    "changed_by_id": 13154473,
    "changed_by_ip": "10.0.0.13",
    "changed_time": "2024-07-08T19:49:36+00:00",
    "type_id": 127,
    "action": "changed",
    "changed_from": "External LMS",
    "changed_to": "Internal LMS",
    "sandbox": true,
    "api_version": null
}
```

## changelog: `GET /changelog`

Results are limited to changes in the past 30 days.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `filter` | no | mixed | See available filter conditions |
| `page` | no | int | The page of results to return if more than 200 records are found. |

**Filter conditions (see overview.md, Filter conditions for reports)**

| Name | Type |
|---|---|
| `changed_by` | object_id |
| `affected` | changelog_affected |
| `affected_type` | choice |
| `type` | choice |
| `action` | enum |

These conditions can be used to narrow the results returned.

**Permissions:** Only keys that have been granted Populi Account Administrator permissions may call this method.

This is a heavy call. For performance reasons, only one heavy call may be made at a time.

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/changelog" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
-d '{
    "filter": []
}' \ 
```

Example response:

```json
{
    "object": "list",
    "count": 108,
    "results": 108,
    "results_per_page": 200,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "change_log",
            "id": 946156435,
            "owner_type_id": 1,
            "owner_id": 16,
            "changed_by_id": 22,
            "changed_by_ip": null,
            "changed_time": "2026-09-21T18:05:04+00:00",
            "type_id": 151,
            "action": "added",
            "changed_from": "Cool Kids Plan",
            "changed_to": null,
            "report_data": {
                "changed_time_local": "2026-09-21 11:05:04",
                "owner_type": "PERSON",
                "change_type": "DEFAULT_TUITION_SCHEDULE",
                "changed_by_id": 22,
                "changed_by_name": "API Dude"
            }
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## Run LMS sync: `GET /lmssync/run`

Begin a sync operation between Populi and an outside LMS (e.g. Canvas), if you have an integration configured.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `direction` | no | enum (from_populi, to_populi, both) | Default is both |

**Permissions:** Only keys that have been granted Populi Account Administrator permissions may call this method.

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/lmssync/run" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{}
```

## logins: `GET /logins`

Results are limited to logins in the past 12 months. See user_username field for the User's username. The username field will only have a value for unrecognized failed logins.

**Parameters**

| Name | Required | Type | Description |
|---|---|---|---|
| `filter` | no | mixed | See available filter conditions |
| `page` | no | int | The page of results to return if more than 200 records are found. |

**Filter conditions (see overview.md, Filter conditions for reports)**

| Name | Type |
|---|---|
| `user` | object_id |
| `username` | text |
| `partial_login` | bool |
| `outcome` | enum |
| `fail_reason` | choice |
| `partial_login_reason` | choice |
| `type` | choice |
| `ip_address` | text |

These conditions can be used to narrow the results returned.

**Permissions:** Only keys that have been granted Populi Account Administrator permissions may call this method.

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/logins" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
-d '{
    "filter": []
}' \ 
```

Example response:

```json
{
    "object": "list",
    "count": 0,
    "results": 0,
    "results_per_page": 200,
    "pages": 0,
    "page": 0,
    "offset": 0,
    "has_more": false,
    "data": [],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```
