# AcademicYear

> Populi API2 model. Source: https://populi.co/api/models/academicyears.html. Docs version: 2026-09-21 11:05:10 PST. Generated by `populi-docs`; do not edit.

## Endpoints

| Method | Path | Action | Summary |
|---|---|---|---|
| GET | `/academicyears` | index | Retrieves all AcademicYear objects. |
| GET | `/academicyears/(academicyear)` | show | Retrieves a specific AcademicYear object. |
| GET | `/academicyears/current` | current |  |

## The AcademicYear object

| Field | Required | Type |
|---|---|---|
| `id` | yes | int |
| `start_year` | yes | int |
| `end_year` | yes | int |
| `num_academic_terms` | no | int |
| `sandbox` | no | bool |
| `api_version` | no | -- |

Example:

```json
{
    "object": "academic_year",
    "id": 4,
    "start_year": 2009,
    "end_year": 2010,
    "num_academic_terms": 2,
    "sandbox": true,
    "api_version": null
}
```

## index: `GET /academicyears`

Retrieves all AcademicYear objects.

**Permissions:** One of the following roles is required to call this method: Academic Admin, Registrar

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/academicyears" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "list",
    "count": 4,
    "results": 4,
    "results_per_page": null,
    "pages": 1,
    "page": 1,
    "offset": 0,
    "has_more": false,
    "data": [
        {
            "object": "academic_year",
            "id": 4,
            "start_year": 2009,
            "end_year": 2010,
            "num_academic_terms": 2
        }
    ],
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## show: `GET /academicyears/(academicyear)`

Retrieves a specific AcademicYear object.

**Parameters:** No additional parameters are needed beyond the ID of the object being requested that appears in the URL.

**Expandable:** `academic_terms`, `academic_super_terms`

**Permissions:** One of the following roles is required to call this method: Academic Admin, Registrar

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/academicyears/(academicyear)" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "academic_year",
    "id": 4,
    "start_year": 2009,
    "end_year": 2010,
    "sandbox": true,
    "api_version": "2026-07-13"
}
```

## current: `GET /academicyears/current`

**Permissions:** One of the following roles is required to call this method: Academic Admin, Registrar

Example request:

```shell
curl "https://yourschool.populiweb.com/api2/academicyears/current" \
-H "Content-Type: application/json" \ 
-H "Authorization: Bearer YOUR_POPULI_API_KEY" \ 
-X GET
```

Example response:

```json
{
    "object": "academic_year",
    "id": 4,
    "start_year": 2009,
    "end_year": 2010,
    "sandbox": true,
    "api_version": "2026-07-13"
}
```
